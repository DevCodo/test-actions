name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - run: npm run build 
  test:
      runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - run: npm test

